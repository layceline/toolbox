[project]
name = "toolbox"
version = "0.1.0"
description = "Workspace for ad-hoc operational tooling."
requires-python = ">=3.14"
dependencies = [
    "authpy",
    "rmq",
]

[tool.uv.workspace]
members = ["packages/*"]

[tool.uv.sources]
authpy = { workspace = true }
rmq = { workspace = true }
